"""cclogger — modular implementation of the claude-session-logger hook.

Phase 0a: empty package skeleton, no code moved yet.
Phase 0b will copy `log-command.py` byte-for-byte to each module file and
trim each to its concern. Until then, this package is empty and `log-command.py`
remains the single source of truth.

The bootstrap line at the top of `log-command.py` adds this directory to
sys.path so `from cclogger.X import Y` resolves correctly.
"""
