# v0.3.8 — Delimiter-Collision Fix: Verification Checklist

Hand-runnable verification for the `__`-in-session-name/shell filename-corruption
fix. Sections 1–5 were executed and PASS on Linux (`oarsapps`, 2026-08-12);
Section 6 is the live-box verification to run with the maintainer.

**Environment prerequisites** (Linux; adjust paths on Windows):
```bash
python3 -m pip install --user --break-system-packages pytest dazzle-filekit
# The hook subprocess loses user-site when HOME is redirected -> always run
# test commands with:
export PYTHONPATH=$HOME/.local/lib/python3.12/site-packages
```

## 1. Baseline integrity (BEFORE reviewing the fix)

- [x] `python3 -m pytest tests/one-offs/ -q` on **unmodified f650334** → `377 passed, 1 skipped`
      (1 pre-existing env failure without the PYTHONPATH export above — not a code failure)
- [x] `python3 tests/snapshots/diff_check.py --capture-baseline` on unmodified code
      → captured **73 files**, of which **66 are corruption artifacts** (multi-`tmux_`
      stacked prefixes up to 8 layers, `--000..--010` fragments) — the unfixed bug
      corrupts even ordinary `__`-free session names when run under tmux.

## 2. Red phase (tests fail on unmodified code for the right reasons)

- [x] `python3 -m pytest tests/one-offs/test_delimiter_collision.py -q` on unmodified
      code → **11 failed, 2 passed** (the 2 passes are the intentional sanity tests:
      clean-transition rename, single-underscore preservation)
- [x] End-to-end stability test reproduces the live pathology exactly: insertion
      layers + `--NNN` fragments, growing between two simulated runs

## 3. Green phase (fix applied)

- [x] `python3 -m pytest tests/one-offs/test_delimiter_collision.py -q` → **13 passed**
- [x] `python3 -m pytest tests/one-offs/ -q` → **390 passed, 1 skipped**
      (one legacy test updated: its fixture name contained `__` and asserted
      passthrough; now asserts the collapsed contract + a delimiter-free length check)
- [x] Snapshot: re-captured baseline post-fix → **15 files** (vs 73), re-run
      `python3 tests/snapshots/diff_check.py` → `Byte-identical to baseline`
- [x] Log CONTENT unchanged: zero content diffs in matched files pre/post fix —
      only filenames healed

## 4. Mutation verification (each fix component is load-bearing)

Each component reverted individually; delimiter suite must go red each time:

| # | Mutation (revert) | Verdict |
|---|---|---|
| M1 | `sanitize_dirname` collapse removed | KILLED (1 failed) |
| M2 | `get_session_name` returns raw | KILLED (1 failed) |
| M3 | `build_session_context` shell collapse removed | KILLED (1 failed) |
| M4 | `extract_session_name_from_file` truncating parse restored | KILLED |
| M5 | rename idempotency guard removed | KILLED (1 failed) |
| M6 | `discover_channel_basenames` greedy `[\w-]+` restored | KILLED (1 failed) |
| M7 | `_embedded_session_name` truncating parse restored | KILLED (1 failed) |
| M8 | unnamed-branch hyphen support removed | KILLED (1 failed) |

- [x] 8/8 KILLED; post-restore full suite green (`390 passed, 1 skipped`)
- [x] M9 (post-adversarial): `get_subtype` underscore sanitization reverted →
      `TestSubtypeBasenames` 2 failed. Restored → suite `392 passed, 1 skipped`

## 4b. Adversarial tester pass (general-purpose agent, unbounded probes)

- [x] 16 hostile-name E2E cases (incl. `____`, 250-char `__`-runs, unicode, spaces,
      backslash/`\1` injection names, `--007` lookalikes, unnamed) — all stable
- [x] Legacy adoption with content-preservation check — converged, no loss
- [x] 5-way concurrent hook subprocesses — no crash, no proliferation
- [x] Rename-transition matrix (hostile↔plain, unnamed→named, name==shell,
      username with `__`) — all converge, no churn on third run
- [x] **Finding 1 (HIGH, fixed + mutation-verified)**: snake_case `subagent_type`
      re-triggered the loop via `_`-bearing subtype basenames (`agents-my_snake_agent`
      → phantom `agents-my`); `get_subtype` now emits `-` for `_`. Tester's own
      repro probe re-run post-fix: OVERALL PASS
- [x] **Finding 2 (MEDIUM, pre-existing, deferred to 0.3.9)**: names ≥ ~200 chars
      crash `claude-history-*.json` construction → silent total logging loss for
      that session. Documented in CHANGELOG "Known issues"
- [x] Windows notes (not runnable here): backslash-in-name dir/file asymmetry;
      MAX_PATH lowers the Finding-2 threshold

## 5. Companion tool (claude-session-backup)

- [x] csb's `sesslog_scanner.py` anchors on `__<uuid>_<user>$` from the right and
      treats the name as opaque → unaffected by collapsed names; no csb change needed.

## 6. Live-box verification (run with maintainer)

- [ ] Back up the plugin cache dir, then copy `hooks/` from this repo over
      `~/.claude/plugins/cache/dazzle-claude-plugins/session-logger/<ver>/hooks/`
      (repo CLAUDE.md "Manual Copy Fallback")
- [ ] In a tmux session whose name contains `_`/`-`, start a throwaway Claude session,
      `/rename` it to a name containing `__`, run several tools, restart once
- [ ] Verify: session dir + files carry the **raw session name verbatim** (`__` and
      all — lossless-name contract, §7); exactly one file per channel;
      `~/.claude/logs/hook-debug.log` shows no repeated `Renamed` storms
- [ ] Existing corrupted dirs (e.g. `SECURITY_REVIEW_2026.05.01__…`): on next session
      event the dir/files converge to the raw-name form; manually move stale
      `--NNN`/malformed leftovers to `<session_dir>/dupes/` (tar backup first)
- [ ] `claude plugin update session-logger@dazzle-claude-plugins` after release
      pulls **0.3.8** (version metadata realigned — was stuck declaring 0.3.6)

## Known non-goals / deferred

- Names containing a literal `--NNN`-like suffix can still confuse
  `find_max_sequence` (pre-existing; note added to #16 FilenameBuilder scope)
- Legacy files whose *shell* portion contains `__` (recorded before shell collapse)
  remain ambiguous to parse; they stop mutating (guards) and adopt on rename
- Windows-specific path behavior not exercised on this box (suite has
  platform-neutral coverage; maintainer's Windows dev box should run Section 3)

## 7. Scope narrowing: lossless names (same release, second pass)

Cross-box analysis (`private/claude/2026-08-12__19-26-23__delimiter-collapse-scope-refinement-lossless-names.md`)
showed the name-side collapse was over-broad: only the machine-generated SHELL field
needs the `__`-free invariant. Name field now passes through verbatim, restoring
name<->session round-tripping. Verified on Linux:

- [x] Suite: **393 passed, 1 skipped** (contract tests re-pointed to lossless names;
      legacy `test_old_limit_would_truncate` reverted to its original expectation;
      new sweep no-quarantine test for raw `__` names)
- [x] Snapshot differential: byte-identical
- [x] Mutation: shell collapse reverted -> tmux contract test red (1 failed); restored -> 16/16
- [x] E2E raw-name round-trip: 3 restart passes with the live repro name -> dir/files carry
      `2026-8-12__zeromeld.org__reddit-slack-fixes` verbatim, 0 fragments, name portion
      copy-pastes into `claude --resume`
- [x] v0.3.8-written data adoption: copy of the real (collapsed) live dir -> converges to
      raw names, 0 fragments, 0 collapsed leftovers, no byte loss, dupes/ preserved
- [x] Interaction noted: raw names re-expose #52 for ~200+ char names (v0.3.8's collapse
      accidentally masked it for `__`-run names). #52 crash-site fix in flight on the
      companion dev box (config.py `Path.exists()` outside try; ENAMETOOLONG re-raised).
